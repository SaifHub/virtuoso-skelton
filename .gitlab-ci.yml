image: docker:latest

variables:
 DOCKER_DRIVER: overlay

services:
- docker:dind

before_script:
- docker info
- apk add --no-cache py-pip
- pip install docker-compose

build:
 stage: build
 script:
 - docker-compose up -d
test:
 stage: test
 script: 
 - docker run kaiwinter/docker-java8-maven mvn clean test

after_script:
 - docker info
