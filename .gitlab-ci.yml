
version: '2'

# @author: ABDUL SAIF
# @date  : 14/11/2016-15/11/2016

# This YAML file Executes Docker and boots up the following:
# Docker, Java, Maven and Selenium Hub.

# Show hidden files: defaults write com.apple.finder AppleShowAllFiles YES,
# Refresh Finder: killall finder

# ted and saifabs can be changed

image: saifabs/ted

before_script:
  - . /xvfb.sh

stages:
  - test
  - coverage

test:
  script: "mvn clean test"
coverage:
  script: "mvn jacoco:report"
